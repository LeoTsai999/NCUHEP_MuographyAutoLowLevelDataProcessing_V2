This is the improved version of V1:

1. Use "Real" TTree structure. The old version is hits-based structure, which is not commonly used in the HEP community.
2. This version also improved the event selection logics, results in a better computational performance.

But the amplidute edition still need to be adjusted. Still hard to fitting with langauss function. Maybe can try use likelyhood fitting rather than chisq-fitting -> Need to learn how to use RooFit!
